{
  "name": "erc20-dapp",
  "private": true,
  "workspaces": [
    "contracts",
    "frontend"
  ],
  "scripts": {
    "compile": "npm run compile -w contracts",
    "test": "npm run test --workspace=contracts",
    "node": "npm run node -w contracts",
    "deploy:local": "npm run deploy:local -w contracts",
    "export-abi": "npm run export-abi -w contracts",
    "dev": "npm run dev -w frontend"
  }
}
