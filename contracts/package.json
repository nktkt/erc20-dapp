{
  "name": "contracts",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat ignition deploy ignition/modules/MyToken.ts --network localhost",
    "export-abi": "hardhat run scripts/export-abi.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "hardhat": "^2.28.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
